{
  "name": "grandsprojets",
  "private": true,
  "version": "1.0.0",
  "description": "Carte interactive des grands projets urbains",
  "scripts": {
    "dev": "netlify dev --port 3001",
    "lint": "oxlint .",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "deploy": "git push"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@supabase/supabase-js": "^2.102.1",
    "dotenv": "^17.3.1",
    "oxlint": "^1.16.0"
  }
}
